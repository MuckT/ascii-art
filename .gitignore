
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

# Gas City
.beads/*
!.beads/identity.toml

# Gas City agent runtime state (per-worktree, never committed)
.gc/
